"use-server"

import prisma from "/app/_lib/prisma";

export const handleEventsOrder = async (newExternalWebhookHotmartReceiverData) => {
    try {
        const eventsOrderData = await {}
    } catch (error) {
        
    }
}